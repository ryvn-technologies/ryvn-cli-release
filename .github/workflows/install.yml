name: Test Ryvn CLI Installation

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]
  workflow_dispatch:

jobs:
  test-installation:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Test Ryvn CLI Installation
        uses: ryvn-technologies/install-ryvn-cli@a3f830a419082f205eb1c0f361d275a9464345da
